# Port Configuration
# IMPORTANT: These ports must stay in sync with backend .env values:
# - VITE_PORT must match backend FRONTEND_PORT (for local dev CORS/CSRF)
# - VITE_API_URL must point to backend API. Required for production: set at build time so login/logout
#   redirect to the API (e.g. https://pp-api.dcorps.dev/api). If unset, logout redirects to current origin.
VITE_PORT=3330
VITE_API_URL=https://pp-api.dcorps.dev/api
